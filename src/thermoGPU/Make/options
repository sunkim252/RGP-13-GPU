EXE_INC = \
    -I$(CUDA_HOME)/include \
    -I/usr/lib/$(shell uname -m)-linux-gnu/openmpi/include

LIB_LIBS = \
    -L$(CUDA_HOME)/lib64 -lcudart \
    -L/usr/lib/$(shell uname -m)-linux-gnu/openmpi/lib -lmpi
